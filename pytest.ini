[pytest]
markers =
    llm: requires a real, reachable LLM backend (Ollama by default). Set MANA_TEST_LLM=1 to run.
    web: requires real network access (ddgs/duckduckgo). Set MANA_TEST_WEB=1 to run.
    slow: takes more than a few seconds (evolution cycles, benchmarks).
testpaths = tests
