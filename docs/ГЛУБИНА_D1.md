# Глубина рассуждения. D1: задача как объект, затем каталог руками

Продолжение `docs/ГЛУБИНА_D0.md`: единица, лестница и прибор описаны там. Лестница после
калибровки — M3, M4, R3–R5; контроли — C0, C1, S; шум — вторая волна.

```text
D0  прибор
 ↓
D1a  новый вычислительный объект не ломает старый
 ↓
D1b  разложение вообще работает
 ↓
D2   MANA учится им пользоваться
 ↓
D3   MANA создаёт новый тип разложения
```

---

## D1a — проверка фундамента

**Единственная гипотеза:** новая абстракция задач семантически прозрачна, когда
подзадач нет. Пустой каталог = плоский поиск. Никакого выигрыша, никакого «глубже»,
никакого T4, никакого `research`.

**Объект** — `mana/discovery/problems.py`:

| Часть | Что это |
|---|---|
| `Problem` | входы, цель, domain (сужение данных), происхождение: родитель, оператор, зависимости |
| `Ledger` | один бюджет на дерево, четыре статьи: поиск, построение вопросов, сборка, проверка |
| `Node`, `Solution` | узел дерева с ответом и ценой; ответ корню, дерево, счёт; используемая глубина по графу зависимостей |
| `Operator` | только форма (вопрос, сборка) |

Непустой каталог до D1b отвергается явно, а не молча игнорируется. Плоский поиск, мера
и вердикт не меняются.

**Проверка тождества** — `scripts/run_d1a.py`. Каждый экземпляр прогоняется дважды:
`policy.run` как есть и `problems.solve` с пустым каталогом.

Сравнивается:
- ответ, в том числе его оценка на мире;
- программа;
- биты: всего, программы, ошибок;
- число вычислений;
- раунды, история;
- где ответ найден впервые;
- статус завершения;
- ошибки на обучении;
- anytime-кривая целиком;
- счёт: все вычисления в статье «поиск», остальные статьи — 0.

Экземпляры:
- **лестница и контроли:** R, S, M по 5 ступеней и C1, сиды 0..9; предел размера —
  размер истины плюс 2, бюджет 400k, как в калибровке — 160 экземпляров;
- **классические:** W0, W3, T1–T4, W4, сиды 0..9, поиск как есть — 70 экземпляров.

### D1a: прогноз до прогона (2026-09-15)

- 230 из 230 экземпляров совпадают во всех полях, включая anytime-кривую и счёт.
- Время интерпретатора и плоского поиска совпадает в пределах шума: медиана отношения
  0.9–1.1.

Любое расхождение означает дефект объекта, а не свойство разложения. Тогда D1a не
пройден: дефект находится и записывается, D1b не начинается.

### D1a: измерено (2026-09-15, прогноз — коммит `1f36069`)

| группа | совпали во всех полях | время объекта / плоского, медиана |
|---|---|---|
| лестница и контроли (R, S, M × 5 ступеней, C1; сиды 0..9) | **160 из 160** | 1.00 |
| классические (W0, W3, T1–T4, W4; сиды 0..9) | **70 из 70** | 1.00 |

Весь прогон — 691 с.

Прогноз сбылся целиком. Поля, которые сравнивались: ответ и его оценка на мире,
программа, биты, вычисления, раунды, история, где найден, статус завершения, ошибки на
обучении, anytime-кривая целиком. Во всех экземплярах счёт бюджета записал все
вычисления в статью «поиск», остальные три статьи — ноль.

**D1a пройден:** задача как объект не меняет старый поиск, когда не задаёт вопросов. На
этом фундаменте D1b может спрашивать, даёт ли разложение выигрыш при том же общем бюджете.

---

## D1b — объявление (до кода)

### Что D1b проверяет — и чего не проверяет

**Вопрос:** даёт ли выигрыш сама рекурсивная форма задачи при заранее заданном каталоге
операторов?

**Не вопрос:** умеет ли конкретный алгоритм хорошо выбирать, как разложить задачу. Это
D2.

**Успех D1b.** Существует строго заданная рекурсивная процедура из каталога D0, которая
при равном бюджете решает больше, чем плоский поиск. Каждый использованный уровень
необходим, на контролях выигрыша нет.

**Не успех:** «мы нашли хороший порядок операторов или хорошую долю бюджета». Иначе
получилось бы «человек придумал план разложения, MANA его выполнила» — и это назвали бы
глубиной MANA.

### Без изменений

- Каталог D0: `остаток`, `остаток-`, `промахи`, `случаи`.
- `новый элемент` — только D3.
- Симметрия ветвей заморожена.
- Бюджет — те же 400k на всё: плоские поиски всех узлов, построение вопросов, сборку и
  проверку.
- Мера, вердикт и плоский поиск — как есть.
- `research` не подключается, T4 не участвует.

### Процедура — одна, записана один раз

Процедура не меняется по семействам, ступеням и результатам запусков. У неё нет доли
бюджета, выбранной рукой: единственный бюджетный параметр — число ветвей каталога.

```text
решить(P, бюджет b, глубина d):
  1. плоский поиск на P — как есть, до своей собственной остановки (терпение), но не
     больше b. Его ответ — R
  2. R точен на domain P  →  P решена, вопросов не задаётся
     d = 5 (верх лестницы)  →  ответ R
  3. остаток бюджета узла делится поровну между ветвями каталога — их три; ветви идут
     в статическом порядке, как операторы записаны в таблице D0, 2.3:
       а) остаток(R):   P' = (цель − R),  решить(P', доля, d+1),  кандидат R + R'
       б) остаток-(R):  P' = (R − цель),  решить(P', доля, d+1),  кандидат R − R'
       в) промахи(R), затем случаи: P' = (цель на промахах R),  решить(P', доля/2, d+1)
          → R'; условие: где права ровно одна из R, R' — решить(Pc, доля/2, d+1)
          → C;  кандидат if(C, R, R')
     Ветвь, у которой вопрос не задаётся (например, у R нет промахов), ничего не тратит
  4. ответ узла — кратчайшее описание из R и кандидатов, по той же мере и с тем же
     порядком при ничьей (биты, размер, неверные точки, текст). Как только ответ узла
     точен на его domain, оставшиеся ветви не запускаются: решённая задача вопросов не
     задаёт
```

Процедура жадная — R у каждого узла один, — но жадность не подогнана под лестницу. Доли не
возвращаются и не переносятся между ветвями: сэкономленное просто не потрачено.

**Счёт** — одно вычисление на каждое вычисление программы на данных узла:
- поиск — вычисления плоских поисков;
- построение вопросов — вычисления R и R' для целей, масок, сужений;
- сборка — вычисление кандидата;
- проверка — вердикт узла.

Всё входит в 400k.

**Эффективная глубина** (D0, 3) считается для каждого решённого дерева сворачиванием:
плоский поиск на задаче внутреннего узла с бюджетом, который съело его поддерево. Это
измерение, его стоимость сообщается отдельно и в бюджет системы не входит.

### Контроль порядка — отчёт, не утверждение

Та же процедура с обратным статическим порядком ветвей (в, б, а) прогоняется на тех же
экземплярах. Если вердикт ядра от порядка меняется, результат D1b записывается как
**зависящий от порядка** — это свойство политики, а не выразительности. В строгом смысле
это не успех D1b.

### Утверждение и ворота

- **Утверждение D1b.** Процедура с каталогом D0 решает экземпляры лестницы при 400k чаще,
  чем плоский поиск; `asserts_domains = (R, M)`.
- **Пары:** M3, M4, R3, R4, R5, сиды 20..29 — 50 пар.
- **Скрытые:** те же ступени, сиды 30..39.
- **Контрпримеры** (плоский решил, процедура нет), сиды 20..29:
  - C0 — R1, M1, S1;
  - C1;
  - S — S2, S3, S4.

  Всего 70 экземпляров.
- **Вердикт** — `core.gates.judge` как есть. Сверх ворот, до прогона:
  - каждая выигранная пара (процедура решила, плоский нет) имеет эффективную глубину
    не меньше 2;
  - контроль порядка не меняет вердикт.
- **Отдельно сообщаются:** цена по четырём статьям, достигнутая глубина по правилу θ = 8
  из 10, решено на ступенях вне утверждения (R2, M2, M5, S5).

### Что будет значить каждый исход

| Исход | Смысл |
|---|---|
| ворота приняли, глубина ≥ 2 на выигранных, порядок не важен | первый положительный результат для настоящего дерева рассуждения |
| оракул D0 решает, процедура — нет | рекурсивная архитектура выразительна, но из доступного результата R операторы нужных подцелей не получают |
| выигрыш зависит от порядка | выигрыш даёт политика, а не форма; вопрос переходит в D2 |
| контрпримеры на C0, C1 или S | рекурсия портит то, что плоский поиск умеет |

### Характеристика плоского поиска на приборе (до прогноза; сиды калибровки 0..9, 400k)

Как плоский поиск тратит 400k: останавливается ли сам (терпение) и сколько бюджета после
него осталось бы ветвям по правилу «сначала плоский поиск до своей остановки».

| ступень | решил | остановился сам | вычислений: мин / мед / макс | осталось ветвям (мед) |
|---|---|---|---|---|
| R1, M1, S1 | 10, 10, 10 | 10, 10, 10 | ~14.6k | решено до ветвей |
| R2 | 5 | 1 | 30k / 400k / 400k | 0 |
| M2 | 9 | 8 | 106k / 107k / 400k | 293k |
| S2 | 10 | 10 | 107k | решено до ветвей |
| **R3** | 0 | **0** | 400k | **0** |
| **R4** | 0 | 7 | 30k / 80k / 400k | 320k |
| **R5** | 0 | 9 | 30k / 30k / 400k | 370k |
| **M3** | 2 | **1** | 30k / 400k / 400k | **0** |
| **M4** | 0 | **0** | 400k | **0** |
| S3 | 10 | 10 | 275k / 276k / 333k | решено до ветвей |
| S4 | 7 | 2 | 272k / 400k / 400k | 0 |
| C1 | 0 | 10 | 61k / 107k / 108k | 293k |

**Следствие для правила бюджета — до прогноза.** По правилу «сначала плоский поиск до
своей остановки» на M3, M4 и R3 вопрос почти никогда не задаётся: плоский поиск
улучшается до самого предела. D1b там по построению равен плоскому поиску. Действовать
процедура может только на R4 и R5 (и вне утверждения — на M2 и C1). Правило, которое
отдаёт ветвям долю с самого начала, отнимает бюджет у плоского поиска. Тогда контроли S3 и
S4, которым плоский поиск тратит 270k–400k, почти наверняка дают контрпримеры.

При равном бюджете выигрыш рекурсивной формы не отделяется от распределения бюджета между
«искать» и «спрашивать». Любое фиксированное правило — уже решение о распределении, и оно
в основном определяет вердикт. Это та же недостающая величина, что нашёл P2d: ценность
исследования.

### Решение владельца: A — утверждение, S1/2 и S1/4 — анализ чувствительности (2026-09-15)

D1b — один эксперимент.

**D1b-A** — единственный заявленный механизм: процедура выше, где плоский поиск узла идёт
до собственной остановки (доля α = 1). Её результат принимается или отклоняется
воротами.

**D1b-S1/2 и D1b-S1/4** — заранее объявленный анализ чувствительности, только
диагностика. Та же процедура, но плоскому поиску каждого узла оставлено не больше α
бюджета узла: α = 1/2 и α = 1/4. Он может остановиться и раньше. Остаток делится
поровну между ветвями, как в A. Правило одно для всех узлов.

**Во всех трёх плечах одинаковы:** каталог, порядок ветвей, общий бюджет 400k, сиды,
проверка. Настройки по промежуточным результатам нет. Победитель после просмотра не
выбирается. Доля, при которой что-то вышло, не называется успехом. Других долей в D1b нет.

**Чувствительность к распределению бюджета — определена до прогона.** Если между тремя
плечами различается знак разности c − b на парах (c — пары, где решила процедура, а
плоский поиск нет; b — наоборот) или прохождение хотя бы одних ворот `core.gates`
(размер выборки, улучшение, значимость, отсутствие регрессии, скрытая выборка,
контрпримеры), результат D1b считается **чувствительным к распределению бюджета**.
Диагностические плечи оцениваются теми же воротами только для этого сравнения. Принять
по ним ничего нельзя.

**Контроль порядка** (обратный порядок ветвей) прогоняется только для A и сообщается, не
утверждается.

**Диагностика прибора, без утверждения.** На ступенях R (прибор знает истину) для
каждого экземпляра сообщается, оказался ли ответ корневого плоского поиска R точным
слагаемым истины или суммой нескольких её слагаемых. Это различает два объяснения
отказа: не хватило бюджета или оператор не может получить подцель из доступного R.

**Вне утверждения сообщаются** ступени R2, M2, M5, S5 (сиды 20..29) во всех плечах.

### D1b: прогноз до кода и до прогона (2026-09-15)

**D1b-A:**
- На M3, M4 и R3 процедура совпадает с плоским поиском на каждом сиде, где тот доходит до
  400k (ожидаемо не меньше 8 из 10 на ступень): вопрос не задаётся.
- На R4 и R5 вопросы задаются на большинстве сидов. Выигрышей всего 0–2: подзадачи-остатки
  похожи на R3 и R4, а доля ветви ~100–120k намного меньше, чем им нужно, даже если R —
  точное слагаемое.
- Пары: c = 0–2 и b = 0. Проигрыш в A невозможен по построению: точный на обучении ответ
  плоского поиска останавливает узел, а неточный и так не был верным. Поэтому вердикт —
  **REJECTED** по улучшению и значимости; скрытая выборка — так же; контрпримеров 0 из 70.
- Контроль порядка вердикт не меняет.

**D1b-S1/2:**
- Проигрыши там, где плоскому поиску нужно больше 200k: M3 (b = 0–2) и S4 — 2–6
  контрпримеров.
- Выигрышей 0–3. Ворота контрпримеров не проходят.

**D1b-S1/4:**
- Контрпримеры на S3 (5–10) и S4 (3–7).
- Выигрышей 0–4. Ворота контрпримеров не проходят.

**Чувствительность:** да. Ворота контрпримеров проходят в A и не проходят в обоих
диагностических плечах.

**Диагностика R:** ответ корневого плоского поиска — точное слагаемое или сумма слагаемых
не больше чем в 3 из 30 экземпляров R3–R5. Кратчайшее описание суммы условий редко
совпадает с одним из её слагаемых.

**Если так, итог:**
- При равном бюджете и одном R на узел рекурсивная форма не окупается ни при одном из трёх
  распределений.
- Там, где разложение получает бюджет, его отнимает у плоского поиска.
- Диагностика R покажет, что упор — в опознаваемости подцелей из R, а не только в
  бюджете.

Это было бы то разделение, о котором говорилось раньше: «оракул решает, процедура нет» —
форма выразительна, но из доступного результата нужных подцелей не получить.

**Статус:** объявление зафиксировано, прогноз записан. Код — следующим шагом.

### Уточнения, появившиеся при реализации (до прогона D1b)

Это не новые решения, а следствия правила «всё в одном бюджете 400k». Их нашёл тест,
когда дерево перерасходовало бюджет (30 004 из 30 000).

- **Узел не создаётся,** если его доля не покрывает его собственные листья. Плоский поиск
  оценивает все листья до того, как смотрит на бюджет, и дерево не может потратить больше,
  чем ему дано.
- **Вопросы не строятся,** если доля ветви не покрывает листья узла: никакая подзадача
  тогда не может быть создана.
- **Каждое списание** на построение вопросов, сборку и проверку делается, только если
  общий бюджет может его оплатить.
- **Использованная глубина** считается по графу зависимостей в узлах и может быть больше
  предела вложенности 5. Условие-подзадача зависит и от узла, и от подзадачи промахов,
  поэтому одна ступень вложенности ветви «промахи и случаи» даёт два звена цепочки.

**Проверка тождества D1a повторена** после переписывания `problems.py` (версия 1.1, с
процедурой D1b):
- 160 из 160 экземпляров лестницы и контролей и 70 из 70 классических совпали во всех
  полях;
- медиана отношения времени — 1.00 и 0.98 (параллельно шёл полный набор тестов).

Пустой каталог по-прежнему — плоский поиск. Все тесты прошли (2250).

### D1b: измерено (2026-09-15; объявление — `6ea1dd5`, код — `7ead9b4`)

Прогон — 1 817 с; бюджет не превышен ни в одном прогоне.

**Решено из 10** (плоский | A | S1/2 | S1/4 | A, обратный порядок):

| набор | ступень | плоский | A | S1/2 | S1/4 | A обр. | A задала вопросы |
|---|---|---|---|---|---|---|---|
| пары | M3 | 1 | 1 | 2 | 0 | 1 | 1 из 10 |
| пары | M4 | 0 | 0 | 1 | 1 | 0 | 0 |
| пары | R3 | 0 | 0 | 0 | 0 | 0 | 3 |
| пары | R4 | 0 | 0 | 0 | 0 | 0 | 5 |
| пары | R5 | 0 | 0 | 0 | 0 | 0 | 8 |
| скрытые | M3 | 1 | 1 | 3 | 2 | 1 | 0 |
| скрытые | M4, R3–R5 | 0 | 0 | 0 | 0 | 0 | 2, 2, 6, 9 |
| контрпримеры | R1, M1, S1, S2 | 10 | 10 | 10 | 10 | 10 | 0 |
| контрпримеры | S3 | 9 | 9 | 9 | **1** | 9 | 1 |
| контрпримеры | S4 | 6 | 6 | **0** | **0** | 6 | 0 |
| контрпримеры | C1 | 0 | 0 | 0 | 0 | 0 | 10 |
| вне утверждения | M2, M5, R2, S5 | 9, 0, 3, 1 | 9, 0, 3, 1 | 9, 0, 4, 0 | 9, 0, 4, 0 | 9, 0, 3, 1 | 0, 2, 0, 1 |

**Вердикты `core.gates`:**

| плечо | статус | непройденные ворота | пары | b / c | скрытые | контрпримеры |
|---|---|---|---|---|---|---|
| **A (утверждение)** | **REJECTED** | улучшение, значимость | 0.02 → 0.02 | 0 / 0 | 0.02 → 0.02 | 0 из 70 |
| S1/2 (диагностика) | REJECTED | значимость, контрпримеры | 0.02 → 0.06 | 0 / 2 | 0.02 → 0.06 | 6 из 70 (все S4) |
| S1/4 (диагностика) | REJECTED | улучшение, значимость, контрпримеры | 0.02 → 0.02 | 1 / 1 | 0.02 → 0.04 | 14 из 70 (S3 8, S4 6) |
| A, обратный порядок | REJECTED | улучшение, значимость | 0.02 → 0.02 | 0 / 0 | 0.02 → 0.02 | 0 из 70 |

- **Чувствительность к распределению бюджета — да:** знак c − b (A 0, S1/2 +1, S1/4 0)
  и набор непройденных ворот различаются между плечами.
- **Контроль порядка** вердикт A не меняет.
- **Выигранных пар у A нет,** эффективная глубина не измерялась.
- **Цена по статьям** (сумма по всем экземплярам):
  - A — поиск 64.6 млн, построение 870, сборка 418, проверка 418;
  - S1/2 — поиск 59.3 млн, построение 44 462, сборка 25 201, проверка 25 201;
  - S1/4 — поиск 47.2 млн, построение 48 846, сборка 27 867, проверка 27 867.

  Сворачивание (вне бюджета): 1.6 млн и 1.4 млн вычислений.
- **Диагностика R:** ответ корневого плоского поиска — сумма слагаемых истины в **0 из
  60** экземпляров R3–R5 (пары и скрытые) во всех трёх плечах.

**Прогноз и результат:**
- Сбылось:
  - A отклонено по улучшению и значимости, выигрышей 0 (прогноз 0–2), контрпримеров 0;
  - контроль порядка не важен;
  - S1/2 — 6 контрпримеров на S4 (прогноз 2–6), выигрышей 2 (прогноз 0–3);
  - S1/4 — S3 8 (прогноз 5–10) и S4 6 (прогноз 3–7);
  - чувствительность — да;
  - диагностика R — 0 (прогноз не больше 3).
- Мимо: A задала вопросы на R3 в 3 из 10 (прогноз — не больше 2).

**Что это значит:**
1. **D1b как утверждение не прошёл.** Заявленная процедура при равном бюджете не решает
   больше плоского поиска. На M3, M4 и R3 она почти не получает ресурса: плоский поиск
   идёт до предела. Там, где получает (R4, R5), не решает ничего.
2. **Упор на R — в опознаваемости, а не в бюджете.** Оракул D0 решает R3–R5 10 из 10,
   когда слагаемые даны. Процедура не решает ни одного экземпляра R ни при каком
   распределении. Ответ плоского поиска на целой задаче ни разу не оказался суммой
   слагаемых. Остаток спрашивается от неверной промежуточной точки: форма выразительна,
   но из доступного результата R нужной подцели не получить.
3. **На M форма может окупиться, но за счёт бюджета.** Когда плоскому поиску оставлена
   половина (S1/2), процедура решает на M на 2 пары и 2 скрытых экземпляра больше. Цена —
   6 экземпляров S4, которые плоский поиск решал только на последней половине бюджета.
   Выигрыш есть лишь там, где ресурс отнят у того, что плоский поиск уже умеет.

   Диагностика после прогона (M3, M4, сиды 20..39; не утверждение) — выигрыши
   диагностических плеч — настоящие многоуровневые деревья:
   - S1/2 — 4 выигрыша, эффективная глубина 4, 2, 5, 4 (использованная 6, 2, 7, 7);
     сворачивание не убрало ни одного уровня;
   - S1/4 — 2 выигрыша глубины 2 и 1 проигрыш (M3, сид 20).

   **Глубина найдена** (3.1): при другом распределении бюджета рекурсивная конструкция
   решает то, чего плоский поиск не решает при 400k, и каждый её уровень необходим. Но
   **не эффективна:** при заявленном распределении (A) её нет, а при диагностических она
   оплачена контрпримерами.
4. **Для D2 не хватает двух вещей, а не одной:**
   - когда спрашивать, а когда продолжать искать — распределение бюджета, линия P2d;
   - **от какого промежуточного результата спрашивать** — не от окончательного ответа
     плоского поиска, а от одного из частичных, которые он видел.

   Оба решения D1b по объявлению принимал фиксированно, и оба оказались решающими.
