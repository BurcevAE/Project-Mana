# Глубина рассуждения. D3: разбор постановки

**Статус:** разбор (разделы 0–8); решение 1 принято, объявление D3-prep-0 — раздел 9.

D3 — это то, что владелец назвал настоящим Н3: MANA сама придумывает, как представлять то,
что стоит исследовать. Контракт `ResearchQuestion → X → DerivedQuestion` для этого готов
(`docs/ГЛУБИНА_ВОПРОС_ИССЛЕДОВАНИЯ.md`, ворота пройдены дважды). Новый вид промежуточного
объекта — это новая программа в каталоге, а не новая ветка кода.

---

## 0. Где мы

| Что | Где | Результат |
|---|---|---|
| рекурсивная форма существует | D0, D1b | оракул решает R3–R5, M3, M4; при другом распределении — деревья эффективной глубины 2–5 на M |
| трасса и шаг P2 — не источник | D2-prep, 1b, 1c | опоры — 3 экземпляра из 50; на R4/R5 — ни опор, ни сокращений |
| глубина вопросов работает | D2-prep-2 | 7 экземпляров с двухуровневой опорой против 2 с одноуровневой; форма «остаток, потом развилка» |
| но из одной точки входа | D2-prep-2 | на каждом экземпляре все опоры начинаются с одного X корня — частичной суммы, которую уже нашёл плоский поиск |
| глубоких задач не достаёт | D2-prep-2 | R4/R5: 0 и при 400k, и при 1.6 млн |

**Узкое место установлено: как находится X.** Каталог не создаёт точек входа, он только
доводит найденные плоским поиском. На глубоких задачах плоский поиск нужной точки входа не
даёт, и ни один заложенный план её не строит.

---

## 1. Что именно изобретается — три уровня

План исследования — три программы (эксперимент, вывод, пересборка). Изобретать можно на
трёх уровнях, и их нельзя смешивать:

| Уровень | Что изобретается | Что остаётся данным |
|---|---|---|
| **D3a** | **программа эксперимента** — как из доступного получить X | вывод и пересборка: обращения операторов каталога (остаток, промахи, развилка) |
| **D3b** | программы вывода и пересборки — новые виды производных вопросов, в том числе «новый элемент» | язык контракта |
| **D3c** | новые примитивы и новые формы значений — расширение языка | интерпретатор-исполнитель |

**Рекомендация — начинать с D3a.** Так велят данные: упор найден именно в том, как
находится X, а производные вопросы (остаток, развилка) уже работают, когда точка входа есть
(D2-prep-2). Изобретать сразу всё — значит не понять, что сработало.

---

## 2. Чем D3 отличается от N2

Из удачных двухуровневых деревьев D2-prep-2 естественно сжать новый план «остаток, потом
развилка» в один. Это **N2 на уровне планов**: пространство растёт из собственных
элементов, как слова шага 3 и ход шага 6. Такой план не создаёт новой точки входа —
он складывает две известные. Полезно, но это не D3 и не Н3. Условие владельца: Н3 не
подменяет N2.

**D3a — новое представление того, что исследовать.** Это программа эксперимента, которая
получает X, какого не даёт ни трасса, ни каталог.

**Проверка новизны** (до прогона):
- X изобретённого плана не совпадает на данных с X ни одного плана каталога ни на одном
  экземпляре, где план что-то дал;
- выигрыш исчезает, если изобретённый план убрать (необходимость).

---

## 3. Варианты генератора

| Генератор | Идея | Сильная сторона | Риск |
|---|---|---|---|
| **G1. Перебор** программ эксперимента ограниченного размера | все программы языка до размера s, отбор по опыту | ничего не подсказано | пространство огромно; отбор по малому опыту переобучается |
| **G2. Сжатие удачных деревьев** | повторяющиеся куски удачных деревьев → новые планы (как ход шага 6) | метод уже принят в проекте (шаг 6, R2c) | это N2 (раздел 2): новых точек входа не даёт |
| **G3. Ретроспекция по собственным дорогим решениям** | на обучающем материале дорогой поиск находит решения; их части, от которых разрешённый вопрос решается дёшево, — это «полезные X». Генератор ищет программу эксперимента, которая получает такие X из доступного **до** решения, дёшево | бьёт прямо в узкое место — «как находится X»; опыт — собственный (как дорогие прогоны R2) | где дорогой поиск ничего не находит, опыта нет; риск выучить форму обучающих семейств |
| **G4. Случайные программы того же размера** | — | — | обязательный **контроль** к любому генератору, как случайные ходы шага 6 и бессмысленная модификация R1 |

**Рекомендация — G3, с G1 как ограниченным пространством поиска и G4 как контролем.**
G3 превращает найденное узкое место в задачу обучения: «вот объекты, от которых вопросы
становятся простыми; найди способ получать их из того, что видно заранее». Решение этой
задачи — программа эксперимента, то есть новое представление того, что стоит
исследовать.

---

## 4. Опыт — откуда и чего нельзя

**Можно (обучающий материал):**
- мелкие ступени лестницы: R2, R3, M2, M3, сиды обучения, отдельные от сидов утверждений;
- классические миры: T1–T3, W0, W3;
- записи исследовательских вопросов (`ResearchRecord`) с этого материала;
- дорогие решения на нём (как дорогие прогоны R2).

**Нельзя:**
- R4, R5, M4, M5 — они для проверки переноса по глубине;
- T4 — изолирован от любого обучения;
- сиды калибровки, утверждений D1b и материал D2-prep, использованный при диагностике.

**Главный тест Н3 — перенос по глубине.** План, изобретённый на мелких ступенях, должен
давать опоры на глубоких (R4/R5, M4/M5), которых не видел. Контракт позволяет плану
применяться рекурсивно, так что глубина — в переносе, а не в обучении.

---

## 5. Критерий отбора изобретённого

Принцип тот же, что у генератора R2 и хода шага 6: лучше та программа эксперимента, в
которой собственный опыт записывается короче. Длина программы плюс цена получить полезные
X по всем записям обучающего материала.

«Ценность» не хранится (контракт): критерий вычисляется из истории и объявляется до
прогона. Никакой настройки по результатам проверки.

---

## 6. Порядок проверок

| Шаг | Вопрос | Как | Утверждение? |
|---|---|---|---|
| **D3-prep-0** | контракт полон для D3a? | в контракте объявлен примитив `candidates(s)` — программы словаря до размера s, — но не реализован. Реализовать и перепрогнать ворота | нет |
| **D3-prep-1: существование** | есть ли вообще в языке программа эксперимента размера ≤ s, дающая воспроизводимые опоры на R4/R5? | поиск по программам эксперимента; истина — только для оценки, на отдельных «сидах существования». Найденное не утверждается и в каталог не входит | нет |
| **D3a: изобретение** | находит ли G3 такую программу из собственного опыта, без R4/R5? | генератор G3 на обучающем материале, критерий раздела 5; контроли G4 (случайные программы того же размера) и генератор на перемешанном опыте | нет |
| **D3a: утверждение** | каталог плюс изобретённый план решают глубокие ступени чаще, чем каталог, при равном бюджете? | `core.gates` на свежих сидах: пары — R4, R5, M4, M5; скрытые — другие сиды; контрпримеры — C0, C1, S, классические миры; перенос — отдельно | **да** |
| **новизна и необходимость** | это новый X, а не переименованный план каталога? нужен ли он? | раздел 2 | часть утверждения |

**Как читать исход D3-prep-1:**

| Существование | Смысл |
|---|---|
| нет программы размера ≤ s | язык контракта недостаточен для нужной глубины; следующий шаг — D3c, расширение языка. Это тоже результат |
| есть | язык достаточен; вопрос D3a — сможет ли MANA найти такую программу сама |

---

## 7. Риски и защиты

1. **Подгонка под лестницу.** Эксперимент вида «перебрать малые условия и взять то, после
   которого остаток проще всего» — это почти вопрос A, только по большему пространству. Если
   G3 изобретёт именно его, это ещё не новое представление. Защиты:
   - проверка новизны (раздел 2);
   - перенос на классические миры и контроли;
   - G4: случайная программа того же размера не должна давать того же.
2. **Ретроспекция требует решений.** Где дорогой поиск ничего не находит (R4/R5), опыта
   нет — по построению. Поэтому обучение только на мелком, а проверка — перенос вглубь.
3. **Ограничение размера s** выбирает человек. Объявляется до прогона, не подбирается;
   исход D3-prep-1 «нет программы ≤ s» относится к этому s.
4. **Цена.** Эксперименты с перебором кандидатов дороги. Всё входит в бюджет (статьи
   контракта), сравнение — при равном бюджете.
5. **Двузначная мера** (D0, 4.5) остаётся свойством меры. Программы эксперимента,
   которым это важно, используют счёт точек.
6. **Честность насчёт Н3.** Если D3a даст результат, это будет новая программа эксперимента,
   изобретённая MANA из собственного опыта, в языке, который написали мы. Новизна —
   относительно каталога и трассы, но не относительно языка. Изобретение самого языка —
   D3c.

---

## 8. Решения владельца

1. **Уровень:** начинать с D3a (изобретение программы эксперимента), оставив вывод и
   пересборку из каталога — или шире?
2. **Генератор:** G3 (ретроспекция по дорогим решениям) с G1 как пространством и G4 как
   контролем — или иначе?
3. **Порядок:** сначала D3-prep-0 (реализовать `candidates(s)` и перепрогнать ворота), затем
   D3-prep-1 (существование на R4/R5, истина только для оценки) — согласны, что
   существование проверяется до изобретения?
4. **Опыт и изоляция:** обучение — R2, R3, M2, M3 и классические миры; проверка — R4, R5, M4,
   M5, контроли; T4 вне всего. Так?
5. **Размер s** программ эксперимента для D3-prep-1 — объявить до прогона: например, до 12
   узлов выражения.

---

## 9. Решение владельца и D3-prep-0: объявление (2026-09-15, до кода)

**Решение 1 принято:** уровень D3a — изобретается программа эксперимента, вывод и
пересборка берутся из каталога. Решения 2–5 открыты. D3-prep-0 от них не зависит; они
нужны до объявления D3-prep-1.

**Обозначения.** Чтобы не путать две величины:
- **s** — размер программы-кандидата на языке ответа, параметр `candidates(s)`;
- **n** — размер программы эксперимента на языке контракта (решение 5, раздел 8).

### 9.1. Что делается

Одна правка интерпретатора контракта (`mana/discovery/questions.py`): примитив
`candidates(s)`. Он объявлен в контракте (`docs/ГЛУБИНА_ВОПРОС_ИССЛЕДОВАНИЯ.md`, раздел 2),
но не реализован.

- **Словарь** — листья и условия узла, ровно `vocabulary` плоского поиска на данных узла.
- **Программы** — все программы грамматики
  `P ::= лист | условие | add(P, P) | sub(P, P) | if(условие, P, P)`
  размера не больше s. Размер — `language.size`: лист 1, условие 3. Первое место `if`
  занимает только условие словаря. Отбор синтаксический: программы с одинаковыми
  значениями не склеиваются.
- **Порядок** — по размеру. Внутри размера: листья, условия, add, sub, if. В составных —
  по размеру левой части, затем в порядке частей. Порядок зависит только от порядка словаря.
- **Цена.** Перечисление бесплатно. Каждая программа, которую эксперимент впервые вычислил
  на данных узла, стоит 1 (статья «построение») — как уже записано в контракте.
- **Ленивость.** Список строится по мере чтения, иначе при s ≥ 7 (миллионы программ) он не
  помещается в память. Для этого:
  - `take(k, L)` берёт первые k элементов, не строя остальных;
  - `argmin` и `argmax` проходят список один раз и сохраняют первое лучшее.

  Для обычных списков выбор и счёт остаются прежними. Это проверяется отдельно (9.2, п. 5).

**Чего правка не делает:** не меняет ни одного плана каталога, не добавляет планов, не
трогает поиск, меру описания и `problems.py`.

### 9.2. Ворота: перепрогон с одной новой проверкой

1. **Смысловая слепота** — существующие тесты; они читают и новую часть исходника.
2. **Каталог — данные, новый примитив тоже.** Новые тесты:
   - план C с `candidates(1)` вместо `leaves` даёт то же дерево, что C, поле в поле:
     кандидаты размера 1 — это листья в порядке словаря;
   - число кандидатов каждого размера равно числу по рекуррентной формуле грамматики, все
     кандидаты различны и собраны из словаря;
   - `take(5, candidates(9))` стоит ровно 5 вычислений и завершается. Без ленивости здесь
     были бы миллиарды программ.
3. **Нулевой случай и D1b через контракт** — `scripts/run_questions_gate.py` без изменений:
   230 и 480 сравнений.
4. **Композиционность** — существующий тест.
5. **Новое: каталог не изменился.** Одноуровневые прогоны D2-prep-2 повторяются и
   сравниваются с его записями (файл результатов прогона `38e4b57`):
   - 8 планов на 60 экземплярах основного плеча, 400k, всего 480 прогонов;
   - A′ пересобирается и сверяется с изменением R2, как в D2-prep-2;
   - сравниваются: прав ли ответ на мире, опора в корне, наблюдение X корня, счёт по
     статьям, число узлов.

   Это проверка, что ленивые `take` и `argmin`/`argmax` ничего не изменили в вопросах A,
   маске C и шаге P2.

**Прогноз:**
- все проверки проходят: 230 из 230, 480 из 480, 480 из 480;
- все тесты проекта проходят;
- время: ворота около 1 900 с, пункт 5 около 1 500 с.

**Правило.** Любое расхождение означает, что правка неверна: её исправляют или отзывают, а
D3-prep-1 не объявляется.

### 9.3. Измерено (2026-09-16; объявление `63f8855`, код `8b29a06`)

| проверка | результат |
|---|---|
| 1. смысловая слепота | тесты проходят, включая новую часть исходника |
| 2. новый примитив — данные | 4 новых теста проходят: число кандидатов по размерам равно формуле грамматики; `candidates(1)` вместо листьев — ровно C, поле в поле; `take(5, candidates(9))` стоит 5 вычислений; `argmin`/`argmax` при равенстве берут первый |
| 3а. нулевой случай | **230 из 230** |
| 3б. D1b через контракт | **480 из 480** поле в поле; деревья те же (медиана 1 / 176 / 206 узлов, максимум 30 / 306 / 315 при долях 1, 1/2, 1/4) |
| 4. композиционность | тест проходит |
| 5. каталог не изменился | **480 из 480**: одноуровневые прогоны D2-prep-2 повторились во всех сравниваемых полях; расхождений 0 у каждого из 8 планов. A′ пересобрана с тем же изменением, что у R2 |

Время: ворота — 1 968 с, пункт 5 — 961 с (прогноз — около 1 500 с). Все тесты проекта
прошли (2 264).

**Прогноз сбылся целиком. Ворота пройдены в третий раз.** В контракте теперь есть
пространство кандидатов, которое не проходит через трассу: программы словаря по размеру,
строящиеся по мере чтения. Каталог, D1b и пустой каталог ведут себя ровно как прежде.

**Следующий шаг — объявление D3-prep-1** (существование программы эксперимента,
дающей опоры на R4/R5). Для него нужны решения 2–5 раздела 8, прежде всего размер n
программ эксперимента.

---

## 10. Решения владельца 2–5 и объявление D3-prep-1 (2026-09-16, до кода)

### 10.0. Решения

2. **Генератора в D3-prep-1 нет.** Полный детерминированный перебор программ эксперимента.
   G3 появляется только в D3a, когда доказано, что искать есть что. Случайные программы
   того же размера остаются диагностическим ориентиром и в вердикт существования не входят.
3. **Один заранее зафиксированный порядок.** Ни луча, ни случайности, ни адаптивного
   отсечения. Обязательно сообщаются: минимальный найденный n, ранг первой годной E в этом
   порядке, сколько выражений рассмотрено и исполнено, полная цена.
4. **D3-prep-1 не пользуется опытом MANA вообще.** Ему известны язык, пространство программ,
   задача и критерий проверки. Опыт получает только D3a: сырые исследовательские записи с
   R2, R3, M2, M3 и классических миров. D3a запрещено передавать списки удачных планов,
   найденные человеком X, названия будущих семейств и метки «полезный X». R4, R5, M4, M5 и
   T4 закрыты.
5. **Лестница размеров** 4, 6, 8, 10, 12 с объявленным потолком; ищется **минимальный**
   размер существующей конструкции. Если при n ≤ 12 и потолке ничего не найдено, вывод —
   «конструкция не найдена при n ≤ 12 и данном потолке», а не «язык не выражает».

**Критерий — две ступени** (заменяет формулировку раздела 6):
- **Существование:** есть E, дающая невырожденный X и выигрыш хотя бы на одном глубоком
  экземпляре.
- **Общность:** та же E работает на нескольких независимых экземплярах, не за счёт одного и
  того же случайного X. Двух разных X, как в D2-prep, не требуется: изобретается программа
  эксперимента, и она вправе извлекать X одного рода из разных состояний.

### 10.1. Что перебирается

Программа эксперимента на языке контракта. Вывод и пересборка берутся из каталога — три
ветви D1b, — потому что по решению 1 изобретается только эксперимент. План ставится
**на корне, один уровень**; ниже — плоский поиск, как в одноуровневых прогонах D2-prep-2.

Перебор идёт по формам значений контракта (P, V, M, N, L). Что какую форму даёт:

| Форма | Операции |
|---|---|
| **P** программа | `own`; `item(L, N)`; `argmin(L, λ)`, `argmax(L, λ)`; `search(V, M, N)` |
| **V** вектор | `target`; `eval(P)`; `bool(M)`; `vadd(V, V)`, `vsub(V, V)` |
| **M** маска | `domain`; `not(M)`; `mask(V)`; `eq(V, V)`, `neq(V, V)`; `and(M, M)`, `or(M, M)` |
| **N** число | константа, `share`; `count(M)`; `bits(P)`; `distinct(V, M)`; `plus`, `minus`, `div`; `probe(V, M, N)` |
| **L** список программ | `leaves`, `conditions`, `held`; `candidates(N)`; `take(N, L)` |

**Ограничения перебора — выбор человека, объявляются до прогона.** Отрицательный исход
относится именно к ним:

1. форма результата E — **P**: вывод каталога читает X как программу;
2. `successors` исключён — ему нужны правила из опыта (решение 4);
3. `flatmap` и `list` исключены — они умножают списки, ничего не оплачивая;
4. `candidates(k)`: не более одного на выражение, не внутри λ, k ≤ 6. Иначе выражение может
   читать миллионы программ, не оплачивая ни одной, и прогон не кончится;
5. λ — только вторым аргументом `argmin`/`argmax`, одна связанная переменная x формы P;
6. константы K = {0, 1, 2, 3, 4, 5, 6, 7, 8, 16} и `share` — доля бюджета узла.

7. **число, которое ничего не читает, — только атом.** `plus(2, 3)` называет то же, что
   называет константа, а вложения такой арифметики — большая часть пространства. Добавлено
   после счёта пространства (10.2) и до любого прогона.

**Выразительность языка и исследованная область языка — разные вещи.** Ограничения 10.1 и
потолки 10.5 задают область, которую проба обходит. Корректный отрицательный итог: «при
n ≤ 12, грамматике 10.1 и потолках 10.5 конструкция не найдена». Писать после этого «язык не
выражает Н3» нельзя: не пройдена вся выразительность, пройдена её объявленная часть.

### 10.2. Порядок и счёт пространства

- **Размер** — число узлов выражения: λ, переменная и константа считаются за узел.
- **Порядок** — по размеру; внутри размера — по объявленному порядку операций (порядок строк
  таблицы 10.1), затем по размеру левой части, затем по порядку частей. Тот же принцип, что
  у `candidates(s)`. Порядок фиксируется до прогона и ни от чего не зависит.
- **Счёт до порождения.** Число выражений каждой формы и размера считается рекуррентно, без
  порождения. Это даёт долю покрытия: исчерпан размер или пройден только его начальный
  отрезок.

**Пространство посчитано до кода пробы** (`mana/discovery/expressions.py`, 2026-09-16):

| размер | выражений формы P | всего до этого размера |
|---|---|---|
| 3 | 33 | 34 |
| 4 | 149 | 183 |
| 5 | 769 | 952 |
| 6 | 3 226 | 4 178 |
| 7 | 29 279 | 33 457 |
| 8 | 162 698 | 196 155 |
| 9 | 1 845 761 | 2 041 916 |
| 10 | 11 317 487 | 13 359 403 |
| 11 | 133 930 136 | 147 289 539 |
| 12 | 865 265 390 | 1 012 554 929 |

Без ограничения 7 те же числа были в 4–5 раз больше (до размера 10 — 56 млн вместо 13,4 млн):
почти всё это — арифметика над константами. Отсюда видно главное: **размеры 11 и 12
недостижимы**, и объявленный потолок должен это признать, а не делать вид, что перебор полный.

### 10.3. Три ступени отбора

Ищется не ещё одна хорошая программа решения, а программа, которая производит промежуточный
объект, благодаря которому решение становится возможным или заметно дешевле. Ступени
устроены под это. **Компактность X сама по себе не проверяется нигде:** X вправе быть длиннее
ответа родителя, если делает подзадачу дешёвой.

| Ступень | Что проверяет | На чём | Проходит, если |
|---|---|---|---|
| **1. допустимость** | эксперимент вообще даёт промежуточный объект | 2 экземпляра скрининга: R4 и R5, сиды 70, 71; бюджет узла 5k; ветвь «остаток» | эксперимент оплачен; X не полное решение родителя (по значениям не равен ни цели, ни собственному ответу узла); производный вопрос строится — его область непуста, а цель не тождественно нулевая |
| **2. локальная полезность** | подзадача или композиция проще исходной задачи | те же экземпляры; дешёвая проба b₀ = 20k, её ставит прибор, а не эксперимент | **либо** проба подзадачи строго короче пробы исходной задачи при том же b₀, **либо** собранная композиция строго короче ответа на исходную задачу, найденного при том же b₀. Истина не участвует |
| **3. настоящая проверка** | решает там, где плоский поиск не решил | ступень 3а: 4 экземпляра существования (R4, R5, сиды 60, 61), 400k, ветвь «остаток». Ступень 3б, подтверждение: 10 экземпляров (сиды 60–69), все три ветви D1b | ответ композиции прав на мире, а плоский поиск при том же бюджете — нет |

**Утверждение существования даёт только ступень 3б.** Первые две — фильтры цены, и они же
диагностика: доля, отсеянная на каждой, показывает, где пространство кандидатов схлопывается.
Ступень 3а — отбор, а не вердикт.

**Квота по размерам** (решение владельца, 2026-09-16). Ступень 3а берёт не «первые N
выживших», а **равное число кандидатов с каждого размера**, объявленное до прогона. Иначе
места достаются мелким выражениям — при доле выживания около 12 % их хватает, чтобы выбрать
весь потолок, — и объявленный прогноз про n = 10 становится логически непроверяемым. Размер
программы — переменная опыта, а не техническая подробность. Внутри размера — объявленный
порядок.

**Дедупликация перед квотой: только точное повторение X.** Разные выражения часто дают один
и тот же промежуточный объект (в дымовом прогоне на 404 прогона пришлось 22 различных X), и
тратить на них квоту незачем. Отбрасывается только точное совпадение представления X —
никакой кластеризации и никаких суждений об эквивалентности по смыслу.

**Ранг — место в целом объявленном порядке**, а не внутри размера. Разбор дымового прогона
показал, что при нумерации внутри размера ранги разных размеров сталкиваются (6 случаев из
119): два разных выражения сливались под одним номером, портя и ключ возобновления, и
объявленное «ранг первой годной E».

**Что с чем сравнивается.** От самой E правильный ответ не требуется: X — промежуточный
объект, а не решение. С плоским поиском при равном бюджете сравнивается композиция
«эксперимент E + вывод каталога + пересборка каталога». Иначе D3-prep-1 выродится в обычный
поиск решения.

Проба исходной задачи при b₀ считается один раз на экземпляр и переиспользуется: это
свойство экземпляра, а не кандидата. Сравнение на ступени 2 идёт при одном и том же b₀ с
обеих сторон.

**Повторы одного и того же X.** Разные выражения часто дают на экземпляре один и тот же
промежуточный объект. Ступень 2 ставится один раз на каждый различный X (по значениям на
точках); остальные выражения получают её ответ и помечаются как повтор. Это мера цены, а не
отбора: вердикт от неё не зависит. Кэш повторов ограничен по памяти, поэтому число
фактических проб сообщается отдельно.

**Чем ограничен эксперимент на ступени 1** (честная граница): бюджет узла там 5k, из них
половина уходит собственному поиску узла, остальное — эксперименту (правило контракта), и
поиск внутри самого эксперимента упирается в эту половину. Эксперименты, которым нужен дорогой внутренний
поиск, на ступени 1 получают бедный X. Это цена массовости; ступень 2 меряет полезность уже
прибором при b₀ = 20k, одинаково для всех.

**Невырожденность** (требование D0): X по значениям не равен ни собственному ответу узла, ни
одному производному ответу. Истина читается только прибором и только для оценки. Сиды
существования (60–69) и скрининга (70, 71) не пересекаются; сиды утверждения D3a — свежие и
здесь не трогаются.

### 10.4. Вердикт

- **Существование:** есть E, у которой хотя бы на одном экземпляре существования ответ прав
  на мире при невырожденной сборке, а плоский поиск при том же бюджете не прав.
- **Общность:** та же E с той же ветвью права на **3 и более** экземплярах, среди которых
  есть и R4, и R5, и X, который она строит, на этих экземплярах не одна и та же программа.

Минимальный n — наименьший размер, на котором выполнено **существование**. Общность
сообщается отдельно и на минимальный n не влияет.

### 10.5. Потолки и учёт

| Что | Потолок |
|---|---|
| статически порождено выражений | 15 млн — это весь размер 10 и всё, что меньше |
| исполнений ступени 1 (потолок скрининга) | 15 млн |
| исполнений ступени 2 | 200 тыс. |
| ступень 3а | квота на каждый размер, одна и та же; число объявляется до прогона по измеренной цене одного прогона на каждом размере, так, чтобы весь объявленный набор уложился в 12 ч |
| подтверждений (ступень 3б) | 100 программ |
| машинное время (потолок по часам) | 12 ч на 6 процессах |

Потолки пересчитаны после счёта пространства (10.2) и до первого прогона: прежний потолок в
1 млн исполнений остановил бы пробу на размере 9, то есть до размера, на котором объявленный
прогноз (10.8) ждёт конструкцию. Размеры проходятся по порядку; прогон останавливается на том
потолке, который наступит раньше, и для каждого размера сообщается, исчерпан он или нет.

**Цена измерена до прогона** (2026-09-16, `scripts/run_d3_prep1_quota.py` и режим
«скрининг»):

| что | измерено |
|---|---|
| скрининг, ступени 1 и 2 | **234 выражения в секунду** на шести процессах (33 457 выражений за 143 с, размеры до 7) |
| один прогон ступени 3а | **10,6 с** процессорного времени, одинаково на размерах с 4 по 10 (размер 3 — 25,2 с) |
| отсюда: все размеры до 9 | 2,4 ч скрининга |
| отсюда: весь размер 10 | **15,9 ч — в 12 часов не помещается** |

Равная численная квота даёт равное вычислительное покрытие: цена прогона от размера не
зависит. Это и была причина мерить.

**Что объявляется до прогона, исходя из этих чисел:**

| статья | сколько |
|---|---|
| скрининг | не больше **6,5 ч**: размеры до 9 исчерпываются (2,4 ч), остаток уходит в начальный отрезок размера 10 — около 3,4 млн выражений из 11,3 млн |
| ступень 3а | квота **200 на размер**: 8 размеров × 200 × 4 экземпляра ≈ 3,2 ч |
| ступень 3б | не больше 100 выражений × 10 экземпляров × 3 ветви ≈ 1,5 ч |
| всего | ≈ 11,2 ч — внутри объявленных 12, потолок не поднимался |

**Честная граница покрытия.** Размер 10 исчерпан не будет. Отрицательный исход на нём
читается только как «не найдено в первых N выражениях объявленного порядка», а не «не
найдено на размере 10». Размеры 11 и 12 не проверяются вовсе.

### 10.5.1. Распределение бюджета по цене (решение владельца, 2026-09-16)

Измерения показали, что **бюджетирование и порядок перебора — часть плана опыта, а не
подробность реализации.** При строгом порядке «размер за размером» размеру 10 доставался
структурный ноль: его съедал размер 9 просто потому, что стоял раньше. Поэтому две величины
разделяются:

| величина | что это | роль |
|---|---|---|
| **охват** | число проверенных выражений, делённое на число выражений в пространстве этого размера | то, что сообщается в исходе |
| **расход** | фактически затраченное вычислительное время | то, чем ограничен прогон |

Ограничение — расход; охват из него следует, а не наоборот.

**Распределение между размерами.** Каждый размер получает равную долю скринингового
бюджета. Размер, исчерпанный раньше своей доли, возвращает неизрасходованное в общий
остаток, и остаток делится поровну между размерами, до которых ещё не дошли.

**Порядок внутри размера — круговой.** Корневые операции идут по очереди, по одному
выражению за круг, и поток, который кончился, выбывает. Внутри корня порядок прежний. Так
начало размера содержит все корневые формы в той пропорции, в какой они есть в
пространстве. Круговой порядок **не означает равенства по расходу**: ветки стоят
по-разному, и представительность здесь — по формам, а не по времени. Это различие
объявляется, а не сглаживается.

**Что имеет право влиять на распределение:**

| можно | нельзя |
|---|---|
| время, вычисления, число пройденных выражений | что найдено, любая оценка кандидата, «лучше/хуже» |

Иначе ступень 2 становится скрытым учителем — ровно то, чего протокол избегает.

**Квота ступени 3а выводится на границе скрининга:**

```
квота = остаток бюджета / измеренная в этом прогоне цена одного прогона
```

Цена измеряется по первым прогонам самой ступени 3а — они настоящие и идут в зачёт, — и не
зависит ни от одного исхода.

**Как это читать в результате.** Не «размер 10 становится проверяемым». Строго:

> размер 10 получает ненулевой, заранее определённый бюджет и, следовательно, ненулевой
> охват пространства.

Охват порядка 8 % не делает отрицательный исход на размере 10 сильным свидетельством того,
что программы такого размера не существует. Он лишь убирает структурный ноль, который
возникал исключительно из-за последовательного распределения бюджета.

**Потолки заморожены.** Потолок скрининга и потолок по часам объявлены здесь и не меняются
после того, как видны первые результаты. Сказать «на n = 10 было интересно, поднимем скрининг
до 5 млн» нельзя: это другая проба, с другим объявлением.

**Сообщается:** минимальный найденный n; ранг первой годной E в объявленном порядке; сколько
выражений рассмотрено и исполнено на каждой ступени; полная цена (вычисления и часы); для
каждого размера — исчерпан он или пройден начальный отрезок, и какая доля. Прогон
возобновляемый, результаты пишутся построчно (урок аварийного отключения).

### 10.6. Диагностический ориентир

Для каждого размера — 1 000 равномерно выбранных корректных по форме выражений (равномерность
даёт та же рекуррентная формула счёта; сид объявляется), тот же скрининг. Сообщается доля
прошедших. **В вердикт не входит:** это мера того, насколько густо полезные программы лежат
в пространстве, и ориентир для D3a.

### 10.7. Сессия 1: промежуточные итоги и остановка скрининга (2026-09-16)

**Решение владельца:** закончить перебор размера 10 досрочно и перейти к ступени 3а.

**Что измерено к моменту остановки** (4 ч 50 мин прогона, шесть процессов):

| размер | выражений | охват | расход | скорость |
|---|---|---|---|---|
| 3–7 | 33 464 | **100 %** | 169 с | — |
| 8 | 162 698 | **100 %** | 2 864 с | 57/с |
| 9 | 1 845 761 | **100 %** | 1 585 с | 1 165/с |
| 10 | 11 317 487 | 3 840 000 — **34 %** | ~13 000 с | 510/с → 1,9/с |

Скорость **не монотонна по размеру**: размер 8 — 57 выражений в секунду, размер 9 — 1 165,
начало размера 10 — 510, конец пройденного отрезка — 1,9. Дорогие формы (`argmin` по
`candidates`, `search` внутри эксперимента) появляются с размера 8, дальше их число почти не
растёт, а дешёвых комбинаций становится на порядок больше. **Экстраполировать ставку с
размера на размер нельзя** — за прогон это опровергло два моих прогноза подряд, в обе
стороны.

**Почему остановка.** За последние 54 минуты скрининга: пройдено 6 000 выражений, новых
промежуточных объектов — **ноль**, новых проб ступени 2 — **ноль**. Оставшаяся доля (101
мин) прибавила бы около 0,1 % размера 10. Ступень 3 при этом временем не стеснена: весь пул
кандидатов помещается в её бюджет целиком.

**Чего это стоит в записи, честно.** Охват размера 10 теперь читается как «скрининг
остановлен решением владельца на 3,84 млн из 11,32 млн», а не «израсходована объявленная
доля». Отрицательный исход на размере 10 слабее ровно на эту величину.

**Главный промежуточный результат — насыщение пространства промежуточных объектов.**

| счётчик | значение |
|---|---|
| допущено к ступени 1 | 3 229 240 |
| из них повтор уже виденного X | **3 225 486** |
| настоящих проб ступени 2 | 3 754 |
| выживших после ступени 2 | 424 507 |
| **различных X в объединении** | **471** |

Различных X по размерам: 11, 7, 14, 44, 97, 261, 316, 84 (размеры 3–10). Ни один X не
крупнее **шести узлов**: 26 листьев, 274 программы в три узла, 125 в пять, 46 в шесть.

**Щель имеет три запора, и все три — наши:**

1. ограничение грамматики 4: `candidates(k)` только до k ≤ 6;
2. бюджет ступени 1: эксперименту остаётся ~2,5 тыс. вычислений, поэтому `argmin` по
   `candidates(6)` (165 тыс. программ) не оплачивается — отсюда 5,4 млн «не исполнилась»;
3. фильтр невырожденности: X, равный собственному ответу узла, отброшен 2,7 млн раз, а
   внутренний поиск при 2,5 тыс. почти всегда возвращает его же. Канал, дающий крупный X,
   закрывает сам себя.

Ширина щели — шесть узлов — это ровно `if(условие, лист, лист)`, то есть **одно слагаемое
R**. По размеру нужная точка входа в щель проходит; значит отрицательный исход скажет и о
выводах каталога, а не только об узости.

**Найденный дефект вывода квоты.** Формула делит остаток на «цену × 4 экземпляра × число
размеров», то есть считает, будто каждый размер израсходует полную квоту. Кандидатов же
всего 834 (471 различных X), и при квоте 160 до ступени 3 дошло бы лишь **268 из 471
различных X**, тогда как проверка всех 471 стоит 1,7–3,6 ч при отведённых четырёх. В сессии
2 квота не связывает: наибольшее число различных X на размере — 316 при потолке 500, значит
**проверяются все 834 кандидата и все 471 X**. Формулу поправить в объявлении следующей
пробы.

**Учёт времени.** Сессия 1 — 4 ч 50 мин (скрининг). Сессия 2 — ступени 3а и 3б, со своим
счётом времени. Полная цена пробы — сумма обеих; отдельные потолки сессий не отменяют
объявленного потолка в 12 ч на весь опыт.

**Вердикта нет.** Существование решается только ступенью 3б, которая в сессии 1 не
запускалась: ни одного прогона при 400k.

### 10.8. Прогноз (до кода и прогона)

| Что | Прогноз |
|---|---|
| размеры 4 и 6 | ступень 3 пуста: выражение такого размера не успевает назвать ни список кандидатов, ни оценку |
| размер 8 | ступень 1 проходят многие, ступень 2 — заметная доля из них, ступень 3 — ни одна |
| ступень 2 | отсеет большинство, но не всё: снижение сложности подзадачи на дешёвой пробе — слабое требование |
| **существование** | выполнится на **n = 10**; ранг первой годной E — миллионы |
| форма первой годной E | выбор по списку кандидатов с дешёвой оценкой по точкам, без вложенного поиска: вложенный поиск не помещается в бюджет |
| **общность** | скорее нет: 3 экземпляра из 10, включая обе ступени, — сильное требование для первой же найденной программы |
| R4 против R5 | если что-то найдётся, то раньше на R4 |
| покрытие | размеры до 9 включительно будут исчерпаны (2,04 млн выражений). **Про размер 10 прогноз «скорее исчерпан» снят до прогона:** измеренная скорость скрининга даёт на него 15,9 ч, и в потолок он не входит — будет пройден только начальный отрезок, около 3,4 млн из 11,3 млн. Значит и объявленный прогноз про существование на n = 10 проверяется лишь на этом отрезке |
| размеры 11 и 12 | не будут пройдены вовсе: 134 млн и 865 млн выражений. О них можно будет сказать только «не проверено» |

### 10.9. Как читать исход

| Исход | Что можно утверждать | Следующий шаг |
|---|---|---|
| найдено при n | язык достаточен; сложность конструкции — n; ранг говорит, насколько трудно её найти перебором | D3a: сможет ли MANA найти её сама, из опыта, без глубоких ступеней |
| не найдено при n ≤ 12 и потолке | **только** «не найдено при ограничениях 10.1 и потолке 10.5». Это не «язык не выражает» | сначала проверить, не виноваты ли ограничения 10.1 (например, запрет на `flatmap`), и лишь затем D3c |
| найдено, но без общности | существует очень специальный план, а не механизм | D3a ставится, но требование общности переносится в утверждение |

---

## 11. Результат D3-prep-1 (2026-09-16)

**Существование не установлено.** Ни один кандидат не дошёл до ступени 3б: выигрышей на
ступени 3а — ноль.

| что | значение |
|---|---|
| кандидатов на ступени 3а | **834** — все 471 различных X, квота не связывала |
| прогонов при 400k | 3 336 (4 экземпляра на кандидата) |
| **выигрышей** | **0** |
| правых на мире | 0 |
| X вошёл в ответ корня | 89 кандидатов (121 прогон) |
| сбоев исполнения | 1 414 из 3 336 (42 %) |
| кандидатов, упавших на всех четырёх экземплярах | **324 из 834 (39 %)** |
| потрачено деревом, медиана | 377 744 из 400 000 |

**Покрытие пространства:** размеры 3–9 — 100 % (2 041 916 выражений), размер 10 — 34 %
(3 846 000 из 11 317 487, скрининг остановлен решением владельца, 10.7). Размеры 11 и 12 не
проверялись.

**Цена:** сессия 1 — 4 ч 50 мин, сессия 2 — 1 ч 7 мин, всего около 6 часов из объявленных 12.

### 11.1. Прогноз не сбылся

В 10.8 я предсказывал: на размерах 4 и 6 — ничего, на 8 — ничего, **существование
выполнится на n = 10**, ранг первой годной программы — миллионы, общность — скорее нет.
Измерено: ноль на **всех** размерах, включая исчерпанные 3–9 и треть размера 10. Сбылась
только отрицательная часть прогноза.

### 11.2. Что можно утверждать и чего нельзя

**Можно:** при грамматике 10.1, потолках 10.5 и этом покрытии в языке контракта не нашлось
программы эксперимента, которая через выводы каталога даёт на R4/R5 ответ, правый на мире
там, где плоский поиск при равном бюджете не прав.

**Нельзя:** говорить, что язык не выражает нужную конструкцию. Не пройдены две трети
размера 10 и целиком размеры 11–12; сверх того **39 % кандидатов упали на всех экземплярах
и настоящей проверки не получили**.

### 11.3. Два ограничения результата, оба измеренные

1. **Щель в шесть узлов** (10.7): все 471 различных X — программы не крупнее шести узлов, и
   ни одного крупнее не возникло в принципе. Три запора — предел `candidates(k ≤ 6)`, бюджет
   ступени 1 и фильтр невырожденности, закрывающий единственный канал крупного X.
2. **42 % сбоев при 400k** против 8 % на первых прогонах: чем крупнее выражение, тем чаще
   оно падает на полном бюджете, хотя проходило при бюджете скрининга. Причина не
   установлена — прибор не различает отказ по форме и отказ по бюджету.

### 11.4. Следующий шаг — не D3c сразу

По таблице 10.9 отрицательный исход велит **сначала проверить, не виноваты ли ограничения**.
Два дешёвых диагностических измерения, каждое объявляется отдельно:

- **разделить «не исполнилась» и «сбой» на отказ по форме и отказ по бюджету** — это скажет,
  упирается ли проба в язык или в цену;
- **расширять щель по одному запору за раз:** `candidates` до 8–10 при неизменном бюджете, и
  отдельно бюджет ступени 1 при неизменном пределе кандидатов.

Только если и после этого ничего не находится, барьер разумно считать выразительным, и
следующим шагом становится D3c.

---

## 12. Диагностики Д1 и Д2 (объявление, 2026-09-16, до кода)

**Решение владельца:** обе диагностики делать. Ни одна не утверждает и не отменяет результат
раздела 11: они объясняют, во что упёрлась проба — в язык или в цену.

Обе идут **отдельным скриптом**; `scripts/run_d3_prep1.py` не трогается, чтобы запись пробы
осталась как есть.

### 12.1. Д1: отказ по форме или отказ по бюджету

Прибор пробы не различал причины: 5,4 млн «не исполнилась» на ступени 1 и 1 414 сбоев при
400k записаны одной строкой. Диагностика разделяет их по классу исключения:

| причина | как выглядит |
|---|---|
| **бюджет** | `_Short` — эксперимент просит больше вычислений, чем осталось узлу |
| **форма** | `TypeError`, `IndexError`, `ValueError`, `KeyError`, `ZeroDivisionError`, `OverflowError` — выражение корректно по формам на бумаге, но на этих данных не исполняется |

**Что меряется:**
- **ступень 1:** 20 000 выражений, взятых равномерно из размеров 7–10 (сид объявляется),
  прогоняются ровно как в пробе — экземпляры скрининга, бюджет узла 5k, — и для каждого
  записывается класс исключения;
- **ступень 3а:** все **324 кандидата**, упавшие на всех четырёх экземплярах, прогоняются
  снова при 400k с той же записью класса.

**Прогноз.** Преобладает форма, а не бюджет: не меньше 70 % отказов на обеих ступенях —
`TypeError` и `IndexError`. Основание: отказ по бюджету контракт ловит сам внутри `_pose` и
записывает как «эксперимент не оплачен», наружу он почти не выходит; наружу выходит то, что
ломается на данных — прежде всего `item` с индексом за пределами словаря, который на
экземплярах существования другой, чем на экземплярах скрининга.

**Как читать:** если преобладает форма — проба упиралась в язык и его исполнимость, и 39 %
непроверенных кандидатов останутся непроверенными при любом бюджете. Если преобладает
бюджет — упиралась в цену, и Д2 обязана это подтвердить.

### 12.2. Д2: какой из трёх запоров держит щель

Все 471 промежуточных объекта — программы не крупнее шести узлов (11.3). Запоров три;
диагностика снимает их по одному, в схеме два на два:

| | бюджет эксперимента 2,5k (как в пробе) | бюджет 40k |
|---|---|---|
| **`candidates(k ≤ 6)`** | клетка А — воспроизводит пробу | клетка Б |
| **`candidates(k ≤ 10)`** | клетка В | клетка Г |

На всех четырёх клетках — **одни и те же** 2 000 выражений, содержащих `candidates`, взятых
равномерно из размеров 8–10 с объявленным сидом. Меряется: сколько различных X даёт клетка и
каковы их размеры.

«Те же выражения при более широком словаре» реализуются так: константа в `candidates(k)`
заменяется на `k + 4` — так 6 становится 10, — а всё остальное в выражении остаётся
нетронутым. Размер выражения при этом не меняется, меняется только то, во сколько программ
ему разрешено заглянуть.

Третий запор — фильтр невырожденности — не снимается: X, равный ответу родителя, не является
промежуточным объектом по определению D0. Его роль уже измерена (2,7 млн отказов) и остаётся
в записи как ограничение постановки.

**Прогноз.**
- Клетка А даёт то же, что проба: X не крупнее шести узлов.
- Клетка В (шире словарь, прежний бюджет) добавляет **мало**: заглянуть в 10-узловые
  кандидаты не на что.
- Клетка Б (прежний словарь, больше денег) добавляет много X, но **по-прежнему не крупнее
  шести узлов**.
- Клетка Г даёт X крупнее шести узлов — впервые за всю серию.

**Как читать:** держит тот запор, снятие которого меняет картину. Если решает Б — дело в
цене, и D3-prep-1 надо переобъявлять с другим бюджетом ступени 1. Если только Г — запоры
держат вместе, и расширение словаря без денег бессмысленно. Если и Г ничего не меняет —
барьер не в щели, а в том, что выводы каталога не умеют воспользоваться никаким X.

**Ни одна клетка не даёт утверждений о существовании:** истина здесь не читается вовсе,
меряются только число и размеры промежуточных объектов.

### 12.3. Промежуточно измерено и поправка к Д2 (2026-09-16)

**Д1 — окончательно.** Отказы на ступени 1 (20 000 выборок, размеры 7–10): исполнилась 8 859,
форма 9 737, бюджет 1 404 — **форма 87,4 % отказов**. Все **324** кандидата, упавших в пробе
везде, при 400k падают с `IndexError` — **форма 100 %**. Прогноз («не меньше 70 % — форма,
прежде всего `IndexError`») сбылся. Смысл: 324 из 834 кандидатов пробы (39 %) не исполняются
на экземплярах существования ни при каком бюджете — словарь там другой, а `item` с
фиксированным индексом этого не переносит. Реально проверено пробой 510 кандидатов.

**Д2, клетки А и Б — окончательно** (1 998 выражений с `candidates`, размеры 8–10):

| клетка | различных X | отказов по бюджету | отказов по форме |
|---|---|---|---|
| А: k ≤ 6, 2,5k | 139 | 85 | 975 |
| Б: k ≤ 6, 40k | 129 | 85 | 997 |

Прогноз для Б («добавит много X») **не сбылся**: шестнадцатикратный бюджет не дал ничего,
отказы по бюджету совпали до единицы. **Запор — не цена.**

**Клетки В и Г первым прогоном не посчитаны.** Они шли больше часа без ответа, один процесс
выделил 89 ГБ памяти при 31 ГБ физической; прогон остановлен по решению владельца, частичных
данных по В и Г нет — клетка отдаёт итог только целиком. Причина: чтение кандидатов в
контракте бесплатно, платится только оценка, и выражение, которое смотрит в `candidates(10)`
через лямбду, ничего не оценивающую, перебирает миллионы программ, держа в памяти все
меньшие размеры. В пробе от этого защищало ограничение 4 (k ≤ 6); диагностика его сняла.

**Поправка, до повторного прогона:** в клетках В и Г одно выражение может прочесть не больше
**200 000** кандидатов без оплаты; сверх этого — отдельный вид отказа, **«чтение»**. Предел
ставится только в скрипте диагностики, библиотека не меняется. Клетки А и Б не
пересчитываются. Число отказов «чтение» само по себе — измерение: оно показывает, сколько
выражений при снятом пределе упирается в бесплатный перебор, а не в деньги и не в форму.

**Оговорка, записанная до результата.** `candidates` выдаёт программы по размеру, и при
словаре узла программ до размера 6 набирается порядка 150 тысяч. Предел в 200 тысяч чтений
кончается около размеров 6–7, поэтому программы размеров 8–10 в клетках В и Г практически
недостижимы — не из-за предела k, а из-за порядка перебора. Отрицательный исход В/Г
объяснение «держит щель» **не снимает**; это слабая проверка.

### 12.4. Д2, клетки В и Г — измерено (с пределом чтения, 251 с)

| клетка | различных X | размеры полученных X | чтение | бюджет | форма |
|---|---|---|---|---|---|
| А: k ≤ 6, 2,5k | 139 | 1, 3, 5 | — | 85 | 975 |
| Б: k ≤ 6, 40k | 129 | 1, 3, 5 | — | 85 | 997 |
| **В: k ≤ 10, 2,5k** | **215** | 1, 3, 5 | **316** | 95 | 520 |
| **Г: k ≤ 10, 40k** | **221** | 1, 3, 5 | **316** | 95 | 521 |

**Что измерено:**
1. **Бюджет не держит нигде.** В ≈ Г, как А ≈ Б: отказы «чтение» и «бюджет» совпадают до
   единицы, число различных X почти то же.
2. **Предел словаря влияет на число X, но не на их размер.** Расширение k с 6 до 10 даёт
   +55 % различных X (139 → 215), но **ни одного X крупнее пяти узлов**. Новые X — это
   малые программы, найденные потому, что `argmin` просматривает более длинный список.
   Прогноз для Г («впервые X крупнее шести узлов») **не сбылся**; по оговорке выше это
   ожидаемо и слабо.
3. **Бесплатное чтение — реальная уязвимость контракта:** 316 из 1 998 выражений (16 %)
   без ограничения 4 уходят в перебор сверх 200 тысяч программ, ничего не платя.
4. Отказов по форме при k ≤ 10 вдвое меньше (975 → 520): длиннее список — реже индекс за
   его пределами.

### 12.5. Итог диагностик

- **Проба упиралась не в цену.** Ни на одной ступени и ни в одной клетке больший бюджет
  ничего не менял.
- **Она упиралась в исполнимость языка.** 39 % кандидатов пробы не исполняются на
  экземплярах существования: `item` с фиксированным индексом не переносит смены словаря.
- **Щель по размеру X не проверена по-настоящему.** Перебор кандидатов по размеру делает
  программы крупнее шести узлов достижимыми только через миллионы бесплатных чтений.

**Следующие шаги — каждый требует отдельного объявления:**
1. правка примитива `candidates`: программы **ровно** размера k (или равномерная выборка из
   них), и, возможно, плата за чтение — тогда крупные X достижимы дёшево, и щель можно
   проверить честно;
2. решение об исполнимости: либо язык не порождает выражений, ломающихся на смене словаря
   (например, индекс берётся по модулю длины списка), либо такие кандидаты считаются
   неприменимыми и честно исключаются из знаменателя вердикта.

Только после проверки щели разумно выбирать между D3b (изобретение выводов) и D3c
(расширение языка).

**Правило.** Никакой подгонки после прогона: ступени, пороги, сиды, порядок и потолки
меняться не будут. Если понадобится другое ограничение, это отдельная объявленная проба.
